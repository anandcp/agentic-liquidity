# Configuration Files

This folder contains configuration files for the empirical panel generation,
simulation environment, and global random seeds.

### Files

- **panel_config.json**  
  Parameters for constructing the synthetic repo–month panel.

- **sim_config.json**  
  Parameters controlling the liquidity-driven multi-agent simulation.

- **seeds.txt**  
  Fixed random seeds for reproducibility across runs.

These files ensure that results produced in the paper can be reproduced by
reviewers or other researchers without modifying code.